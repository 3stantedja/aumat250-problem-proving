\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{problems}[2019/10/02 Problem Writing]
\RequirePackage[utf8]{inputenc}
\RequirePackage[margin=1in]{geometry}

% \RequirePackage{tikz}
\RequirePackage{graphicx}
\RequirePackage{wrapfig}
\RequirePackage{booktabs}
\RequirePackage{titling} % Expose titling data for general use
\RequirePackage{fancyhdr}
\RequirePackage{enumitem}
\RequirePackage{datetime2}

\RequirePackage{microtype}
\RequirePackage{lmodern}
\RequirePackage{inconsolata}

\RequirePackage{mathtools}

\RequirePackage{amsmath}
\RequirePackage{amsthm}
\RequirePackage{amssymb}
\RequirePackage{textcomp}
\RequirePackage{siunitx}

\RequirePackage[parfill]{parskip}
\RequirePackage{array}
% \RequirePackage{multirow}

\newcommand{\tlambda}{\(\lambda\) }
\newcommand{\ttheta}{\(\theta\) }
\newcommand{\lequiv}{\Leftrightarrow}

\theoremstyle{definition}
\newtheorem*{problem}{Problem}

% Define an 'answer' environment based on the
% 'proof' environment
% https://tex.stackexchange.com/questions/123689/duplicating-the-amsthm-proof-environment
\newenvironment{answer}{\begin{proof}[Answer]}{\end{proof}}

\sisetup{
  separate-uncertainty,
  multi-part-units = brackets,
  bracket-numbers,
  sticky-per
}
\numberwithin{equation}{section}
\numberwithin{figure}{section}
\numberwithin{table}{section}

\DTMsetup{
  datesep = {},
  datetimesep = .,
  timesep = {},
  showzoneminutes = false,
  showisoZ = true % Show time in 24-hr format
}

% Set ordered lists to be (a), (b), etc...
\renewcommand{\labelenumi}{(\alph{enumi})}
\pagestyle{fancy}
\fancyhead[L]{\thetitle}
\fancyhead[C]{\thedate}
\fancyhead[R]{\theauthor}
\fancyfoot[L]{Build \DTMnow} % Really rough versioning
\fancyfoot[C]{\thepage}

\microtypesetup{
  tracking = true,
  kerning = true
}

\author{Laurence Amadeus Tristan}
\endinput